<%- model_class = Chofer -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:Nombre) %></th>
      <th><%= model_class.human_attribute_name(:Apellido) %></th>
      
      <th><%= model_class.human_attribute_name(:DNI) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @chofers.each do |chofer| %>
      <tr>
        <td><%= link_to chofer.id, chofer_path(chofer) %></td>
        <td><%= chofer.Nombre %></td>
        <td><%= chofer.Apellido %></td>
        <td><%= chofer.DNI %></td>
        <td>
          <%= link_to t('.Editar', :default => t("helpers.links.edit")),
                      edit_chofer_path(chofer), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.Eliminar', :default => t("helpers.links.destroy")),
                      chofer_path(chofer),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Estaas seguro?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_chofer_path,
            :class => 'btn btn-primary' %>
